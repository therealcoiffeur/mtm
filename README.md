# mtm
MTM is a local attack surface mapping for macOS. It recursively walks the filesystem, identifies Mach-O binaries, captures file metadata and code-signing entitlement (XML), stores the results in SQLite, and includes browser-based views for exploring the collected data.
